# Assets — shared across landing pages

Brand-level assets used by more than one landing page (logo marks, shared
texture/noise overlays, generic Ecom College imagery) live here rather
than being duplicated per page. Page-specific imagery belongs in its own
`/public/landing/<page>/` folder instead.
