# מדריך עריכת תוכן - אקדמיית איקום

מדריך זה מיועד לכל מי שצריך לערוך טקסטים, שאלות מבחן, סרטונים או צבעים במערכת - **בלי** לגעת
בקוד ובלי צורך בידע בתכנות. כל התוכן שהמשתמש רואה נמצא בתיקיית `content/`, בקבצי `JSON` ו-`MDX`
פשוטים לעריכה.

> **חשוב:** אחרי כל שינוי בקובץ תוכן, שמרו את הקובץ. אם האתר כבר פועל במחשב (`npm run dev`),
> השינוי יופיע בדפדפן אוטומטית תוך שניות.
>
> קבצי JSON חייבים להישאר בפורמט תקין (מרכאות, פסיקים, סוגריים מסולסלים). אם תשברו את הפורמט,
> האתר יציג שגיאה ברורה שאומרת בדיוק איזה קובץ מקולקל - זה לא באג, זו הגנה מכוונת שמונעת מהאתר
> "להישבר בשקט".

---

## שאלות נפוצות ("רוצה לשנות X? תפתחו את Y")

**רוצה לשנות את כותרת קורס הסייבר?**
פתחו את `content/courses/cyber/meta.json`

**רוצה לערוך את נושא 1 בקורס הסייבר (Playbook)?**
פתחו את `content/courses/cyber/topics/topic-1.mdx`

**רוצה להחליף את הסרטון של עוז בקורס הסייבר?**
פתחו את `content/courses/cyber/video.json` ושנו את השדה `url`

**רוצה לערוך את שאלות המבחן בקורס הסייבר?**
פתחו את `content/courses/cyber/quiz.json`

**רוצה לשנות את הצבעים של קורס הסייבר?**
פתחו את `content/courses/cyber/theme.json`

**רוצה לשנות את "מי זאת איקום"?**
פתחו את `content/site/about-ecom.json`

**רוצה לשנות את פריטי התפריט הצדדי?**
פתחו את `content/site/navigation.json`

**רוצה לשנות את מסלול ההכשרה (השלבים ב"מסלול ההכשרה שלי")?**
פתחו את `content/site/training-path.json`

**רוצה לשנות טקסטים בעמוד הבית/דשבורד?**
פתחו את `content/site/home.json`

**רוצה לשנות את תוכן עמודי "בקרוב" (פרופיל לקוח / שיטת המכירה / סימולציות AI)?**
פתחו את `content/site/customer-profile.json`, `content/site/sales-method.json` או
`content/site/simulations.json`

**רוצה לערוך את חוויית הפתיחה הממותגת "מי זאת Ecom" (11 המסכים שמופיעים אחרי ההתחברות)?**
פתחו את הקבצים תחת `content/site/ecom-intro/` - ראו הסבר מלא בהמשך המדריך.

---

## מבנה תיקיית content/

```
content/
  site/
    navigation.json        פריטי התפריט הצדדי
    home.json               טקסטים כלליים בדשבורד
    about-ecom.json          כל התוכן של "מי זאת איקום"
    training-path.json       שלבי "מסלול ההכשרה שלי"
    customer-profile.json    עמוד "בקרוב" - פרופיל לקוח
    sales-method.json        עמוד "בקרוב" - שיטת המכירה
    simulations.json         עמוד "בקרוב" - סימולציות AI

    ecom-intro/               חוויית הפתיחה "מי זאת Ecom" (9 מסכים) - ראו הסבר מלא בהמשך
      brand.json
      welcome.json
      ariel.json
      industry.json
      stats.json
      alumni.json
      instructors.json
      trust.json
      closing.json
      audio.json
      students.json

  courses/
    cyber/
      meta.json              שם הקורס, תת-כותרת, תיאור קצר
      theme.json              הצבעים של הקורס
      overview.json            טקסט המבוא של הקורס
      video.json               פרטי סרטון עוז
      quiz.json                שאלות מבחן הידע
      topics/
        topic-1.mdx           נושא 1 ב-Playbook
        topic-2.mdx           נושא 2 ב-Playbook
        topic-3.mdx           נושא 3 ב-Playbook
    ai/            (אותו מבנה בדיוק)
    fullstack/      (אותו מבנה בדיוק)
    ux-ui/           (אותו מבנה בדיוק)
    digital-marketing/  (אותו מבנה בדיוק)
```

כל חמשת הקורסים בנויים **באותו מבנה בדיוק** - ברגע שהכרתם קורס אחד, אתם מכירים את כולם.

---

## עריכת קובצי JSON

קובץ JSON נראה כך:

```json
{
  "slug": "cyber",
  "title": "Cyber",
  "displayName": "קורס Cyber",
  "subtitle": "אבטחת מידע והגנת סייבר",
  "shortDescription": "איך פורצים, איך מגנים, ולמה כל חברה היום חייבת אנשי סייבר.",
  "icon": "ShieldCheck",
  "order": 1
}
```

חוקים פשוטים לעריכה:

- מותר לשנות רק את הטקסט שבין המרכאות `" "`.
- **אין** למחוק מרכאות, פסיקים (`,`) או סוגריים מסולסלים (`{ }`).
- השורה האחרונה לפני `}` **לא** מסתיימת בפסיק.
- אל תשנו את שמות השדות (המילים לפני `:`, כמו `"title"`) - רק את הערכים אחריהן.
- שדה `"slug"` הוא מזהה טכני של הקורס (למשל `cyber`) - **אל תשנו אותו**.

### שדה `icon`

בכמה קבצים (למשל `meta.json`, פריטי ניווט, "חוזקות" ב"מי זאת איקום") יש שדה `icon` עם שם
אייקון באנגלית, לדוגמה `"ShieldCheck"`, `"Building2"`, `"Bot"`. השמות האלה מגיעים מספריית
[Lucide Icons](https://lucide.dev/icons) - אפשר לחפש שם אייקון אחר באתר ולהחליף, אבל צריך
לוודא עם מפתח/ת שהאייקון קיים ברשימת האייקונים הזמינים במערכת (`lib/icon-map.ts`), אחרת
יוצג אייקון ברירת מחדל כללי במקומו.

---

## עריכת מבחן הידע (quiz.json)

מבחן הידע של כל קורס בנוי בפורמט קבוע: **5 שאלות אמריקאיות** (10 נקודות כל אחת) ו-**שאלה
פתוחה אחת** (50 נקודות) - בסך הכל 100 נקודות. המבנה הזה **נבדק אוטומטית** בכל הפעלה של
האתר (dev / build) - אם הוא לא מדויק (למשל 4 שאלות אמריקאיות במקום 5, או סכום נקודות שאינו
100), האתר יציג שגיאה ברורה במקום להיטען עם תוכן שגוי.

### מבנה כללי של הקובץ

```json
{
  "title": "מבחן ידע - Cyber",
  "passScore": 80,
  "intro": {
    "title": "לפני שמתחילים",
    "description": ["פסקת מבוא ראשונה.", "פסקת מבוא שנייה."],
    "reminder": "תזכורת קצרה שמוצגת בתיבה בולטת במסך הפתיחה.",
    "materialsNote": "אפשר להיעזר בחומרי הלמידה (Playbook והסרטון) במהלך המבחן.",
    "goodLuck": "בהצלחה!"
  },
  "questions": [ /* ראו למטה */ ],
  "closingNote": "טקסט סיום שמוצג בשאלה האחרונה. אפשר להשתמש ב-{{courseName}} והוא יוחלף אוטומטית בשם הקורס."
}
```

- `passScore` - הציון (באחוזים, מתוך חלק השאלות האמריקאיות בלבד) הנדרש כדי לעבור את המבחן.
- `intro` - כל הטקסטים במסך הפתיחה של המבחן. `description` היא רשימת פסקאות (אפשר להוסיף
  או להסיר פסקאות). `materialsNote` הוא המקום שמבהיר לנציג/ה שמותר להיעזר בחומרי הלמידה
  תוך כדי המבחן - **אין למחוק שדה זה**.
- `closingNote` - מוצג רק ליד השאלה האחרונה (מסמן לנציג/ה שהמבחן מסתיים בקרוב). המחרוזת
  `{{courseName}}` בתוך הטקסט מוחלפת אוטומטית בשם הקורס בזמן טעינה - אפשר להשאיר אותה או
  להסיר אותה, אבל לא לשנות את התחביר של הסוגריים הכפולים אם משתמשים בה.

### שאלה אמריקאית (type: "multipleChoice")

```json
{
  "id": "q1",
  "type": "multipleChoice",
  "points": 10,
  "question": "איך הכי נכון להסביר ללקוח מה זה סייבר?",
  "options": [
    { "id": "a", "text": "תחום שמלמד רק איך לפרוץ למחשבים של אנשים" },
    { "id": "b", "text": "תחום שמגן על מערכות מידע, רשתות וארגונים מפני תוקפים" },
    { "id": "c", "text": "קורס תיאורטי בלבד ללא יישום מעשי" },
    { "id": "d", "text": "תחום שמתאים רק למי שכבר תכנת בעבר" }
  ],
  "correctOptionId": "b",
  "explanation": "סייבר עוסק בהגנה על מערכות מידע - לא רק בתקיפה."
}
```

- `points` בשאלה אמריקאית חייב להיות `10` (5 השאלות האמריקאיות ביחד = 50 נקודות).
- `correctOptionId` חייב להיות זהה ל-`id` של אחת מהתשובות ברשימת `options` (למשל `"b"`).
  **התשובה הנכונה לעולם לא מוצגת לנציג/ה לפני שהוא/היא שולח/ת את המבחן** - זהו מידע פנימי
  בלבד בקובץ התוכן.
- `explanation` הוא הסבר קצר שיוצג לנציג/ה **רק אחרי שליחת המבחן**, ליד תשובה זו.
- חייבות להיות בדיוק **5** שאלות מסוג `multipleChoice` בכל `quiz.json`.

### שאלה פתוחה (type: "openText")

```json
{
  "id": "q6",
  "type": "openText",
  "points": 50,
  "question": "איך היית מסביר/ה ללקוח פוטנציאלי את הערך של הקורס במילים שלך?",
  "rubric": [
    "10 נקודות - מזכיר/ה את הבעיה שהקורס פותר עבור הלקוח",
    "10 נקודות - משתמש/ת בשפה ברורה, לא טכנית מדי",
    "10 נקודות - לא מבטיח/ה תוצאה שאי אפשר להתחייב אליה"
  ]
}
```

- חייבת להיות בדיוק **שאלה פתוחה אחת** בכל `quiz.json`, ששווה `50` נקודות (משלימה ל-100
  עם ה-50 של חמש השאלות האמריקאיות).
- לשאלה פתוחה **אין** `options` ואין `correctOptionId` - במקום זה יש `rubric`: רשימת קריטריונים
  להערכה (מקובל לפרק אותם ל-5 סעיפים של 10 נקודות כל אחד, כפי שמופיע בכל `quiz.json` קיים).
  **ה-rubric הוא מידע פנימי בלבד ולעולם לא מוצג לנציג/ה** - הוא נשמר יחד עם התשובה לצורך
  הערכה עתידית (ידנית או אוטומטית).
- תשובות פתוחות **אינן** נבדקות אוטומטית. הן נשמרות במצב "ממתין להערכה" (`pending`) יחד עם
  הרוברייק, ולא מקבלות ציון עד שמישהו/משהו יעריך אותן. **חשוב:** אין למערכת מנגנון שממציא
  ציון לשאלות פתוחות - הציון הסופי המוצג לנציג/ה מבוסס רק על 50 הנקודות של השאלות האמריקאיות,
  והשאלה הפתוחה מוצגת בנפרד כ"ממתינה להערכה".

### כללים משותפים

- `id` של כל שאלה (`q1` עד `q6`) חייב להיות **ייחודי** בתוך הקובץ.
- סדר השאלות ברשימה `"questions"` הוא סדר ההצגה בפועל למשתמש/ת - השאלות האמריקאיות (`q1`-`q5`)
  מופיעות ראשונות והשאלה הפתוחה (`q6`) אחרונה.
- **אין** לשנות שאלה קיימת מסוג `multipleChoice` לסוג `openText` (או להפך) בלי לתאם עם מפתח/ת,
  כי זה משנה את מבנה 5+1 הקבוע ויגרום לשגיאת ולידציה.
- לא ניתן להוסיף או להסיר שאלות באופן חופשי (כמו בעריכה הישנה) - מבנה 5 אמריקאיות + שאלה
  פתוחה אחת הוא קבוע ונבדק אוטומטית.

---

## עריכת נושאי ה-Playbook (קבצי .mdx)

תוכן ה-Playbook של כל קורס מבוסס במדויק על ה-PDF המאושר של אותו קורס - אותו סדר, אותה
היררכיה ואותה הדגשה, רק כחוויית למידה דיגיטלית במקום PDF. קובץ נושא נראה כך:

```
---
title: "מהו עולם הסייבר?"
keyPoints: []
---

## שני העולמות המרכזיים בסייבר

כפי שמופיע בחומרי הקורס, עולם הסייבר מתחלק לשני תחומים מרכזיים: **סייבר הגנתי**
ו**סייבר התקפי**. שני התחומים שונים בגישה שלהם, אך משלימים זה את זה.

<CustomerExplain>

"תחשוב על מאבטח בקניון. התפקיד שלו הוא למנוע מאנשים לא מורשים להיכנס ולגרום נזק."

</CustomerExplain>

<SalesTip title="טיפים חשובים">

- סייבר אינו רק פריצה למחשבים.
- המטרה המרכזית היא להגן על ארגונים ועל המידע שלהם.

</SalesTip>
```

לקובץ שני חלקים:

1. **החלק העליון** בין שתי השורות `---` (נקרא "Frontmatter") - כאן משנים רק את `title`
   (כותרת הנושא). את `keyPoints` משאירים ריק (`[]`) - התוכן החשוב מוצג ישירות בתוך הטקסט
   באמצעות התיבות המתוארות למטה, בדיוק באותו מקום שבו הוא מופיע ב-PDF המאושר.

2. **החלק התחתון** (אחרי ה-`---` השני) - הטקסט הארוך של הנושא עצמו. אפשר לכתוב פסקאות רגילות,
   ולהשתמש בסימנים הבאים:
   - `## כותרת משנה` - יוצר כותרת משנה בתוך הטקסט (מקביל לכותרת המודגשת והמסומנת בקו תחתון
     ב-PDF). `###` יוצר כותרת משנה קטנה יותר, מקוננת בתוך `##`.
   - `- טקסט` - יוצר נקודה ברשימה.
   - `**טקסט**` - מדגיש מילה **בהדגשה**, בדיוק כמו טקסט מודגש ב-PDF.

### התיבות המיוחדות (בהתאמה מלאה ל-PDF המאושר)

ה-PDF המאושר כולל כמה סוגי קטעים מיוחדים שחוזרים על עצמם - אנחנו הופכים כל אחד מהם לתיבה
מעוצבת בדף באמצעות תגית מיוחדת. **חשוב:** תמיד להשאיר שורה ריקה אחרי התגית הפותחת ולפני
התגית הסוגרת, אחרת הטקסט לא יוצג נכון.

**"איך מסבירים את זה ללקוח?"** - כל קטע כזה ב-PDF הופך ל:

```
<CustomerExplain>

הטקסט של ההסבר ללקוח, בדיוק כפי שמופיע ב-PDF.

</CustomerExplain>
```

**"טיפ לנציג המכירות" / "טיפים חשובים" / "מה חשוב לזכור?"** - כל קטע כזה הופך ל:

```
<SalesTip title="הכותרת המדויקת מה-PDF">

הטקסט של הטיפ, כולל רשימות עם `- ` אם יש.

</SalesTip>
```

שימו לב ל-`title` - יש להעתיק לתוכו את הכותרת המדויקת כפי שהיא מופיעה ב-PDF לאותו קטע ספציפי
(היא משתנה בין "טיפ לנציג המכירות" ל"טיפים חשובים" וכו', בהתאם למה שכתוב שם).

**תפקיד/מסלול קריירה** (בנושא 3 של כל קורס) - כל תפקיד הופך לכרטיס נפרד:

```
<CareerRole title="שם התפקיד המדויק מה-PDF">

תיאור התפקיד...

<CustomerExplain>

אם יש הסבר ללקוח ספציפי לתפקיד הזה, הוא מקונן כאן בפנים.

</CustomerExplain>

</CareerRole>
```

**פסקה שכולה מודגשת ב-PDF** (הגדרה מרכזית או משפט מודגש בולט, בלי כותרת מעליו) - הופכת ל:

```
<HighlightedConcept>

הפסקה המודגשת, בדיוק כפי שמופיעה ב-PDF.

</HighlightedConcept>
```

אם אתם מוסיפים תוכן חדש שלא היה ב-PDF המקורי (עדכון עתידי, תיקון, הרחבה), אין חובה להשתמש
בתיבות האלה - אפשר גם רק פסקאות רגילות עם `##` ו-`**הדגשה**`. התיבות המיוחדות קיימות כדי
לשחזר במדויק את המבנה של ה-Playbook המאושר.

אין צורך לגעת בשום דבר טכני נוסף בקובץ - רק בטקסט עצמו.

---

## עריכת צבעי קורס (theme.json)

```json
{
  "primary": "#2952E3",
  "secondary": "#1E3A8A",
  "soft": "#EAF0FF",
  "border": "#C7D6FF",
  "glow": "rgba(41, 82, 227, 0.28)",
  "textAccent": "#1D3FBF",
  "gradientFrom": "#2952E3",
  "gradientTo": "#16215C"
}
```

כל צבע הוא קוד hex (למשל `#2952E3`) או `rgba(...)`. אם תרצו לשנות את גוון הקורס, מומלץ להחליף
את כל שבעת הצבעים יחד (לא רק את `primary`) כדי לשמור על הרמוניה חזותית - למשל, על ידי לקיחת
כל הצבעים מתמונת הכריכה הרשמית של הקורס. אם אתם לא בטוחים איך לגזור פלטת צבעים תואמת, מומלץ
להתייעץ עם מעצב/ת או עם מפתח/ת לפני השינוי.

---

## הוספת קורס שישי (למתקדמים)

1. בחרו slug באנגלית לקורס החדש (למשל `product-management`).
2. צרו תיקייה חדשה תחת `content/courses/<slug>/` עם הקבצים: `meta.json`, `theme.json`,
   `overview.json`, `video.json`, `quiz.json`, ותיקיית `topics/` עם `topic-1.mdx`, `topic-2.mdx`,
   `topic-3.mdx` - הכי קל להעתיק תיקיית קורס קיים ולערוך את התוכן.
3. ב-`meta.json` החדש, ודאו ש-`"slug"` תואם בדיוק לשם התיקייה, ושה-`"order"` הוא המספר הבא
   בתור (למשל `6`).
4. פנו למפתח/ת כדי להוסיף את ה-slug החדש לרשימת הקורסים בקובץ `lib/courses.ts` (שורה אחת) -
   זהו השלב היחיד בתהליך שדורש נגיעה בקוד.

---

## עריכת חוויית הפתיחה "מי זאת Ecom" (content/site/ecom-intro/)

חוויית הפתיחה הממותגת שמופיעה לכל נציג/ה חדש/ה מיד אחרי ההתחברות, לפני הדשבורד - **לא** אותו
דבר כמו עמוד "מי זאת איקום" הרגיל בתפריט הצדדי. זו "מיני-מצגת" של 9 מסכים שנועדה ליצור רושם
ראשוני, גאווה ואמון לפני תחילת ההכשרה בפועל. כל 9 המסכים ניתנים לעריכה מלאה בלי לגעת בקוד, דרך
הקבצים תחת `content/site/ecom-intro/`:

| קובץ | מסך | מה עורכים |
|---|---|---|
| `welcome.json` | 1 - ברוכים הבאים | כותרת, `supportingLine` (משפט המשנה - ראו הערה על שבירת שורה למטה), טקסט כפתור "מתחילים" |
| `ariel.json` | 2 - אריאל | טקסט הפיקוח הפדגוגי, `benefits` (רשימת יתרונות הפיקוח - זה התוכן המרכזי במסך), נתיב לוגו אריאל |
| `industry.json` | 3 - חיבור לתעשייה | כותרת, טקסט, `logosCaption` (משפט קטן מתחת לקיר הלוגואים, אופציונלי), רשימת לוגואים (שם + נתיב תמונה) - הלוגואים מוצגים בפיזור חופשי, בלי מסגרות |
| `stats.json` | 4 - Ecom במספרים | כותרת + רשימת נתונים (הראשון ברשימה מוצג כמספר הראשי/גדול); לכל נתון `value` (המספר), `suffix` (למשל `"%"` או `"+"`), `decimals` (מספר ספרות אחרי הנקודה, למשל `1` עבור `4.8`) ו-`label`. הכרטיס עם המילה "Google" ב-label מקבל אוטומטית את סמל Google, והכרטיס "חברות ועסקים בקשרי השמה" מציג כמה מלוגואי industry.json |
| `alumni.json` | 5 + 6 - סיפורי הצלחה וסרטוני בוגרים | כותרות שני המסכים + רשימת בוגרים משותפת (`name`, `subtitle`, `course`, `photoSrc`, ואופציונלי `videoSrc`, `quote`, `story`, `roleBefore`, `roleAfter`, `company`) |
| `instructors.json` | 7 - המרצים שלנו | כותרת, טקסט, רשימת מרצים (`name`, `photoSrc`, ואופציונלי `course`, `role`, `description`, `company`) |
| `trust.json` | 8 - סיבות לסמוך על Ecom | כותרת + רשימת פריטי אמון, כל אחד עם `headline` ו-`explanation` - מוצגים כארבעה בלוקים נפרדים עם אייקון |
| `closing.json` | 9 - סיום | כותרת, `body` (הפסקאות המרכזיות, מוצגות במסגרת אחת - הפסקה האחרונה מודגשת בסגול), `onboarding` (`eyebrow` + `steps` - רשימת הבלוקים "מה קורה עכשיו"), טקסט כפתור הסיום |
| `audio.json` | (כל החוויה) | `"src"` - נתיב לקובץ מוזיקת רקע (למשל `"/ecom-intro/audio/theme.mp3"`), או `null` אם עדיין אין קובץ. אין צורך לגעת בקוד כדי להוסיף מוזיקה - מספיק להעלות קובץ ל-`public/ecom-intro/audio/` ולעדכן את הנתיב כאן |
| `students.json` | (מסכים אינפורמטיביים נבחרים) | רשימת תמונות "סטודנטים" גנריות (לא בוגרים/מרצים אמיתיים בשם) לפי `id` מקום (`welcome-1`, `welcome-2`, `ariel-1`, `industry-1`, `stats-1`, `trust-1`, `closing-1`, `closing-2`). כל עוד אין תמונה עם `id` מסוים, המסך המתאים פשוט לא מציג שם תמונה - שום דבר לא "נשבר" |

**תמונות ווידאו**: כל התמונות והסרטונים של החוויה נמצאים תחת `public/ecom-intro/` (לפי נושא:
`brand/`, `ariel/`, `companies/`, `alumni/`, `community/`, `students/`). כדי להחליף תמונה - מעלים
קובץ חדש לתיקייה המתאימה ומעדכנים את הנתיב (`photoSrc` / `videoSrc` / `src` / `logoSrc`) בקובץ
ה-JSON הרלוונטי כך שיצביע לקובץ החדש (הנתיב תמיד מתחיל ב-`/ecom-intro/...`, בלי המילה `public`).

**חשוב לגבי בוגרים, מרצים ולוגואים**: אין להוסיף שם, לוגו, ציטוט, תחום הוראה או "תפקיד לאחר
הלימודים" שלא אושרו בפועל - כל שדה כזה ב-`alumni.json`, `instructors.json` ו-`industry.json` הוא
אופציונלי; אם פרט מסוים לא קיים, פשוט משמיטים אותו מהקובץ במקום להמציא אותו. תמונות "סטודנטים"
מ-`students.json` הן דמויות גנריות בלבד - לעולם לא מוצגות כבוגר/ת, מרצה או אדם אמיתי בשם.

**הערה על `welcome.json` -> `supportingLine`**: המשפט הזה בנוי כך שהוא נשאר בשורה אחת במסכים
רחבים, ועובר לשורה שנייה בדיוק במקום הנכון (אחרי הפסיק, לפני "בואו") כשאין מספיק מקום - זה
מושג עם רווחים בלתי-שוברים (non-breaking spaces) בין כל שאר המילים. אם עורכים משפט זה, הכי בטוח
להעתיק את המבנה הקיים ולהחליף רק את המילים, ולא להקליד רווחים רגילים חדשים בתוך המשפט.

מספר השאלות/הבוגרים/המרצים/הלוגואים בכל מסך גמיש - אפשר להוסיף או להסיר פריטים מהרשימות (`stats`,
`logos`, `stories`, `instructors`, `benefits`, `items`, `photos`) בלי לגעת בקוד, כל עוד כל פריט
שומר על אותו מבנה כמו הפריטים הקיימים.

---

## מה **לא** לגעת בו

- שמות השדות בקבצי JSON (המילים שלפני `:`).
- ערך `"slug"` בכל קובץ `meta.json`.
- מבנה קובצי ה-`.mdx` (שתי השורות `---`).
- כל דבר תחת `app/`, `components/` או `lib/` - אלה קבצי קוד, לא קבצי תוכן.

אם משהו לא ברור או שהאתר מציג שגיאה אחרי עריכה - שווה תמיד לבדוק קודם שלא נמחקו בטעות מרכאות,
פסיקים או סוגריים.
